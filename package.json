{
  "name": "pokt-browser-wallet",
  "description": "POKT Browser Wallet",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "homepage": "https://github.com/decentralized-authority/pokt-browser-wallet#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/decentralized-authority/pokt-browser-wallet.git"
  },
  "keywords": [],
  "author": "Ryan Burgett <ryan@burgettdev.net>",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --concurrency 20",
    "test": "turbo run test",
    "test:account-utils": "turbo run test --filter=account-utils",
    "test:util": "turbo run test --filter=util",
    "build": "turbo run build",
    "force-build": "turbo run build --no-cache",
    "clean": "turbo run clean",
    "build:ext": "turbo run build --filter=ext",
    "build:ext-ui": "turbo run build --filter=ext-ui",
    "build:background": "turbo run build --filter=background",
    "build:content": "turbo run build --filter=content",
    "build:ui": "turbo run build --filter=ui",
    "bundle": "turbo run bundle",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "eslint": "^7.32.0",
    "eslint-config-custom": "*",
    "fs-extra": "^11.1.1",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "packageManager": "npm@9.6.7",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
